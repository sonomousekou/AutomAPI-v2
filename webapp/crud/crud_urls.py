from django.urls import path
from .views import *

urlpatterns = [
    # Autres URLs de votre projet
]

# Ajoute les URLs générées dynamiquement
urlpatterns += [
    # Les URLs générées pour le CRUD des modèles
]

